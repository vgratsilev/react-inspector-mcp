export const CreateElementOnly = () => {
  return <section>Create element only</section>;
};

export const PropOnly = () => {
  return <section>Prop only</section>;
};

export const RouteConfigOnly = () => {
  return <section>Route config only</section>;
};

export const RegistryOnly = () => {
  return <section>Registry only</section>;
};

export const ObjectRegistryOnly = () => {
  return <section>Object registry only</section>;
};

export const DynamicOnly = () => {
  return <section>Dynamic only</section>;
};
